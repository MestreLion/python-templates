# Trimmed down version of https://github.com/github/gitignore/blob/main/Python.gitignore
# Only actual artifacts generated by this project's workflow using current tools
# Do NOT add personal developer files and directories, such as IDE settings!
# These to to either .git/info/exclude (preferably using git's templatedir feature),
# or a single '*' in the dir's .gitignore, as done by modern mypy in .mypy_cache/

# Byte-compiled / optimized / DLL files
__pycache__/

# Distribution / packaging
dist/
*.egg-info/

# Environments
.env
venv/
